# Agent Roles

Agent roles define authority boundaries for multi-agent workflows.

The model is intentionally lightweight. Roles do not require a runtime scheduler; they define who may change what, who reviews what, and when L0 synchronization is required.
